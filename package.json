{
  "name": "playwright-test",
  "version": "1.0.0",
  "description": "Test project for Playwright",
  "main": "test.js",
  "scripts": {
    "postinstall": "playwright install chromium --with-deps",
    "start": "node test.js"
  },
  "dependencies": {
    "playwright-core": "^1.40.0",
    "express": "^4.17.1"
  }
}