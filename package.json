{
  "name": "playwright-test",
  "version": "1.0.0",
  "description": "Test project for Playwright",
  "main": "test.js",
  "scripts": {
    "postinstall": "playwright install --with-deps && playwright install-deps",
    "start": "node test.js"
  },
  "dependencies": {
    "playwright": "^1.0.0",
    "express": "^4.17.1"
  }
}